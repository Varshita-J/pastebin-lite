# Core Django + REST framework
Django==5.2.5
djangorestframework==3.14.0
drf-yasg==1.21.7

# PostgreSQL driver (binary version, avoids compilation)
psycopg2-binary==2.9.11

# Short UUID for paste IDs
django-shortuuidfield==0.1.3

# Optional: Rich text editor for pastes
django-ckeditor-5==0.2.18

# Optional: CORS headers for API
django-cors-headers==3.14.0

# JWT auth (if you use token-based auth)
djangorestframework-simplejwt==5.2.2

# Utility for environment variables
python-dotenv==1.0.0

# Optional: Database URL config (if using DATABASE_URL)
dj-database-url==2.1.0
